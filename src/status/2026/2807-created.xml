<item>
   <title>Issue #2807 created</title>
   <pubDate>2026-07-21T16:57:40Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2807</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2807</guid>
   <description>&lt;div&gt;&lt;p&gt;Dynamic node tests: static errors?&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;Copied from https://github.com/qt4cg/qtspecs/pull/2712#pullrequestreview-4615985800, something we should at least discuss:&lt;/p&gt;
&lt;hr/&gt;
&lt;p&gt;The rules of this PR combined with #2734 have the potential to break ordinary XML paths. A parser may rewrite &lt;code&gt;&amp;lt;xml/&amp;gt;/data()&lt;/code&gt; to:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;&amp;lt;xml/&amp;gt;/(
  if (. instance of node()) then name() else (
    if (. instance of node()) then (
      let $A as ... := fn(){ data() }() return ...
    ) else (
      let $A as ... := fn(){ data() }() return ...
  )
)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;As &lt;code&gt;XPTY0004&lt;/code&gt; can be raised statically, &lt;code&gt;fn(){ data() }&lt;/code&gt; may result in a static error, and the full query will be rejected although the paths in question would never be executed.&lt;/p&gt;
&lt;p&gt;Maybe we should stress that the presented equivalent code is to be understood as equivalent code that cannot be simply pasted by the parser (?). Or we simply drop the equivalent representation and liimt ourselves to formal rules.&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
