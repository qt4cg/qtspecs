<item>
   <title>Issue #2923 created</title>
   <pubDate>2026-09-21T10:11:31Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2923</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2923</guid>
   <description>&lt;div&gt;&lt;p&gt;Idempotent Coercion&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;We already discovered that coercing a function F to a required type T may be necessary even if F is already an instance of T, because the coerced function performs stronger checks on the types of the arguments supplied (see issue #1020).&lt;/p&gt;
&lt;p&gt;We now have a test case:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;test-case name="recordTypeDecl-050" covers-40="PR1874"&amp;gt;
      &amp;lt;description&amp;gt;Record types: field order, subtyping and coercion&amp;lt;/description&amp;gt;
      &amp;lt;created by="Christian Gruen" on="2026-09-16"/&amp;gt;
      &amp;lt;test&amp;gt;&amp;lt;![CDATA[
         declare record local:AB(a as xs:integer, b as xs:integer);
         declare record local:BA(b as xs:integer, a as xs:integer);
         declare function local:f($r as local:BA) { map:keys($r) };
         local:f(local:AB(1, 2))
      ]]&amp;gt;&amp;lt;/test&amp;gt;
      &amp;lt;result&amp;gt;
         &amp;lt;assert-deep-eq&amp;gt;"b", "a"&amp;lt;/assert-deep-eq&amp;gt;
      &amp;lt;/result&amp;gt;
   &amp;lt;/test-case&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;which demonstrates that coercing a record R to type T may be necessary even if R is already an instance of T, because it changes the order of entries in the record (the &lt;code&gt;instance of&lt;/code&gt; check does not require fields to be in the right order).&lt;/p&gt;
&lt;p&gt;This feels very counter-intuitive to me. On an ordinary English-language usage, "coercion" means modifying something to meet a given requirement, and shouldn't be necessary if the requirement is already met. If the order of fields is important enough to justify re-ordering, then it should also be important enough for &lt;code&gt;instance of&lt;/code&gt; to fail.&lt;/p&gt;
&lt;p&gt;I would like to see if we can adopt a general principle that if a value V is an instance of a type T, then coercion of V to T should be idempotent. This principle is important to allow type checking to be performed statically.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;(There's an argument that if a function expects a record of type R, then the implementation of the function body should be able to make assumptions about the order of fields, in order to enable more efficient access. But on that line of reasoning, "instance of" should be a stronger test, so that the same logic can be used (say) in a branch of a typeswitch).&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;This may involve looking again at coercion of function items, which I previously thought was the only exception to this rule.&lt;/p&gt;
&lt;p&gt;Another way of solving the problem for function calls would be to change the "instance of" rules. For example, given the example from the current spec,&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;let $f as function(xs:integer) as item()* := function($x) { $x + 1 }
return $f(12.3)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;we would need to change the definition of "instance of" so that &lt;code&gt;function($x) { $x + 1 }&lt;/code&gt;  is no longer an instance of &lt;code&gt;function(xs:integer) as item()*&lt;/code&gt; (instead, it is merely coercible to that type). As far as I can tell, that simply means removing the "contravariance" provision on argument types (§3.3.2.6 rule 2(f)). It seems a reasonable change, because the supplied function doesn't do everything that the expected type requires - it doesn't reject non-integer arguments.&lt;/p&gt;
&lt;p&gt;An alternative solution would be to revert to the 3.1 spec here: no coercion is performed in this case and the above query succeeds, returning 13.3.&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
