<item>
   <title>Issue #2924 created</title>
   <pubDate>2026-09-22T06:13:42Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2924</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2924</guid>
   <description>&lt;div&gt;&lt;p&gt;NodeTest for JNodes&lt;/p&gt;&lt;div class="markup"&gt;&lt;h3&gt;JNode node test discussion&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;We could also redefine the node test &lt;code&gt;*&lt;/code&gt;, when applied to JNodes, so it only selects a JNode whose jvalue is a singleton map or array; to select other nodes, use the node test jnode(). That's a good analogy to the fact that with XNodes, &lt;code&gt;*&lt;/code&gt; only selects elements.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;em&gt;Originally posted by @michaelhkay in &lt;a href="https://github.com/qt4cg/qtspecs/issues/2922#issuecomment-5761864152"&gt;#2922&lt;/a&gt;&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;Does that mean that &lt;code&gt;foo&lt;/code&gt; selects only &lt;code&gt;jnode(foo, (array(*)|map(*)|sequence(*)) )&lt;/code&gt; (or sort of)?&lt;/p&gt;
&lt;p&gt;In that case, we would need something like &lt;code&gt;atom(bar)&lt;/code&gt; or &lt;code&gt;@bar&lt;/code&gt; to select &lt;code&gt;jnode(bar, *)[not(child::jnode(*))][not(self:(array(*)|map(*)) )]&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;That probably makes sense.&lt;/p&gt;
&lt;p&gt;There is a differences here compared to XML. In XML, typically, most child nodes that have a name are elements, whereas most child nodes that are not elements are nameless.  However, in map/array structures, most atomic nodes have a name, as well as not atomic nodes.&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
