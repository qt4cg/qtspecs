<item>
   <title>Issue #2526 created</title>
   <pubDate>2026-03-17T08:44:48Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2526</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2526</guid>
   <description>&lt;div&gt;&lt;p&gt;Facets&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;I propose enhancing the item type syntax to allow facets (as in XSD) to be defined for atomic types.&lt;/p&gt;
&lt;p&gt;For example&lt;/p&gt;
&lt;p&gt;&lt;code&gt;declare item type cal:month as xs:integer with (minInclusive=1, maxInclusive=12);&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;&lt;code&gt;declare variable $quote-character as xs:string with (minLength=1, maxLength=1) external;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;The semantics are exactly as if a restricted type with the same facets were imported from a schema.&lt;/p&gt;
&lt;p&gt;At least initially:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;facets are allowed only on atomic types and not on xs:untypedAtomic&lt;/li&gt;
&lt;li&gt;the value of the facet must be a Constant&lt;/li&gt;
&lt;li&gt;we only allow facets that restrict the value space (therefore not whitespace)&lt;/li&gt;
&lt;li&gt;we allow the pattern facet only on xs:string and xs:anyURI so it becomes a restriction on the value space&lt;/li&gt;
&lt;li&gt;we don't allow the enumeration facet (though we could consider using it to replace our current enum type)&lt;/li&gt;
&lt;li&gt;we only allow one value for each facet (so no multiple patterns)&lt;/li&gt;
&lt;li&gt;we drop some of the dubious combinations such as defining length facets on xs:QName.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;In the subsumption rules, the atomic type &lt;code&gt;A with (FACETS)&lt;/code&gt; is a subtype of A.&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
