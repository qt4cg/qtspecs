<item>
   <title>Issue #2703 created</title>
   <pubDate>2026-06-23T16:24:53Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2703</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2703</guid>
   <description>&lt;div&gt;&lt;p&gt;Dynamic selectors: consistent error handling&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;The enclosed-expression selector &lt;code&gt;axis::{ E }&lt;/code&gt; coerces &lt;code&gt;E&lt;/code&gt; to &lt;code&gt;(xs:QName | xs:string)*&lt;/code&gt; for XNodes, but to &lt;code&gt;xs:anyAtomicType*&lt;/code&gt; for JNodes, where any non-matching key simply selects nothing. This asymmetry means the same selector &lt;code&gt;$x/child::{ $keys }&lt;/code&gt; raises a type error when &lt;code&gt;$x&lt;/code&gt; is an element, yet silently matches nothing when &lt;code&gt;$x&lt;/code&gt; is a map or array.&lt;/p&gt;
&lt;p&gt;I think we should handle the XNode case leniently as well: atomic items in the selector value that are not &lt;code&gt;xs:QName&lt;/code&gt; or &lt;code&gt;xs:string&lt;/code&gt; values are simply ignored (they match nothing), as was already stated in a note for the former &lt;code&gt;get()&lt;/code&gt; selector. This keeps the two node kinds parallel and avoids surprising, kind-dependent failures when the key sequence is computed generically. Raising no error would also live up to the XPath 1.0 tradition.&lt;/p&gt;
&lt;p&gt;If we want to keep the current error handling, we should also reject other cases in which the resulting selector makes no sense:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;$node/child::{ 1 }      (: ↯ :) 
$node/child::{ "x y" }  (: ✓ currently accepted, even though the string is an invalid local name :) 
$array/child::{ x }     (: ✓ currently accepted, even though arrays cannot have string keys :) 
&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
