<item>
   <title>Pull request #2858 created</title>
   <pubDate>2026-08-21T23:35:30Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/pull/2858</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2858</guid>
   <description>&lt;div&gt;&lt;p&gt;2853 Default values of function parameters&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;Fix #2853&lt;/p&gt;
&lt;p&gt;This resolves the issue as follows:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;In both XSLT and XQuery, the expression defining the default value of a function parameter (for a user-defined function) is evaluated in the same way as the initializing expression of a global variable. We don't constrain the expression, but it has no access to the static or dynamic context of the function caller, and it defines a fixed default that applies to all calls on that function.&lt;/li&gt;
&lt;li&gt;However, as a special case there is provision to define that the context item/value of the caller is used as the default. In XQuery this is done by defining the default using the custom syntax &lt;code&gt;$node as node() := context value&lt;/code&gt;. In XSLT we use &lt;code&gt;select="current()"&lt;/code&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
