<item>
   <title>Issue #2821 created</title>
   <pubDate>2026-07-31T09:08:11Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2821</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2821</guid>
   <description>&lt;div&gt;&lt;p&gt;XHTML5 serialization: prefix normalization&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;We're getting a failure in XSLT test output-038 as a result of changing the default XHTML version to 5.0. The element&lt;/p&gt;
&lt;p&gt;&lt;code&gt;&amp;lt;one:h3&amp;gt;xxxx&amp;lt;/one:h3&amp;gt;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;is being serialized as&lt;/p&gt;
&lt;p&gt;&lt;code&gt;&amp;lt;one:h3 xmlns:one="http://ns.example.com" xmlns=""&amp;gt;xxxx&amp;lt;/one:h3&amp;gt;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;The expected results do not include the namespace undeclaration &lt;code&gt;xmlns=""&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;The reason we have added the namespace undeclaration is that XHTML5 serialization introduces prefix normalization, including the rule&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;Any namespace node for any of those three namespaces that was previously present on any element node in the [input tree] is also removed, unless the prefix that that namespace node declared is used as the prefix on the name of an attribute on that element or an ancestor of that element.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;The element, prior to serialization, had a namespace node &lt;code&gt;(""="http://www.w3.org/1999/xhtml")&lt;/code&gt;, which we have removed under this rule, and removing the namespace node has resulted in the serializer outputting a namespace undeclaration.&lt;/p&gt;
&lt;p&gt;Possibly the phrase "or an ancestor of that element" is intended to cover this, but it's ambiguous (does it mean "or on the name of an attribute of an ancestor of that element?"). Logically, the purpose of the rule is to prevent unwanted namespace declarations.&lt;/p&gt;
&lt;p&gt;Note: the serialization spec in this area is essentially unchanged between 3.1 and 4.0. The only change is that XHTML5 is now the default.&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
