<item>
   <title>Issue #2647 created</title>
   <pubDate>2026-05-20T12:16:43Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2647</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2647</guid>
   <description>&lt;div&gt;&lt;p&gt;file:descendants: recursion, filtering&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;It is advisable for an implementation (even if this is not addressed in the spec) to evaluate &lt;code&gt;file:descendants&lt;/code&gt; fully before returning results. Otherwiese, a file walker may become corrupt, for example when a directory is deleted while it is being traversed. On the other hand, this means that the filtering can only take place after the full result is available.&lt;/p&gt;
&lt;p&gt;Other programming languages differentiate between filtering the directories to traverse and the results to return, and I think we should do the same. My proposal:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;file:descendants(
  $path     as xs:string
  $options  as record(
    recurse  as (fn(xs:string, xs:integer) as xs:boolean?)? := true(),
    filter   as (fn(xs:string, xs:integer) as xs:boolean?)? := true()
  )? := {}
) as xs:string*
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;With &lt;code&gt;recurse&lt;/code&gt;, the directories to be traversed can be selected; with &lt;code&gt;filter&lt;/code&gt;, the actual results can be reduced. The &lt;code&gt;xs:integer&lt;/code&gt; value indicates the current position; this way, traversal can for example be aborted after the first &lt;em&gt;n&lt;/em&gt; directories.&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
