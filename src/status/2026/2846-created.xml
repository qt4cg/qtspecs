<item>
   <title>Issue #2846 created</title>
   <pubDate>2026-08-17T19:06:21Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2846</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2846</guid>
   <description>&lt;div&gt;&lt;p&gt;file:append and file:write serialization&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;Both file:append and file:write leave the serialization format implementation-defined if no serialization parameters are supplied. This is explicit in the 4.0 spec, implicit in the 1.0 spec. But test cases have to make assumptions, for example EXPath-file-append2-002 assumes XML serialization (with or without an XML declaration). EXPath-file-append2-001 outputs four atomic values and assumes they will be space-separated, which has no justification in the spec. EXPath-file-appendText2-001 makes multiple calls on file:append() and assumes the values are appended with no newline or separator: again, I don't think there's anything in the spec to justify this assumption.&lt;/p&gt;
&lt;p&gt;An implementation that chose JSON serialization would probably fail all the test cases.&lt;/p&gt;
&lt;p&gt;Furthermore, even if serialization options are specified, it's unclear whether file:append should add a newline after the appended content. (This is also true, though less important, for file:write). That's because none of the serialization methods (with the possible exception of canonical XML/JSON) says explicitly whether the serialized output is followed by a final newline.&lt;/p&gt;
&lt;p&gt;If multiple calls on append() don't add a newline (as suggested by EXPath-file-appendText2-001) this means that any required newline separators must be added manually; but even the effect of &lt;code&gt;file:append(char('\n'))&lt;/code&gt; is implementation-defined; appending &lt;code&gt;char('\d')||char('\a')&lt;/code&gt; may cause the CR to be escaped as &lt;code&gt;&amp;amp;#x0D;&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;It's also unclear how you append a simple string to a file without getting any escaping of special characters (except of course by using a different function such as file:append-text()). All the available serialization options do some kind of escaping. Yet surely, appending a string without escaping is the most common use case!&lt;/p&gt;
&lt;p&gt;I think my preferred solution would be to say that in the absence of serialization options, we append the result of &lt;code&gt;string-join($value, ' ')&lt;/code&gt; to the file. That's certainly going to break some existing applications, but since the current spec is so implementation-defined, it's hard to see how to do better. It merely says that $value is appended to the file in some unspecified way. An implementation that appended &lt;code&gt;string-join($value, ' ')&lt;/code&gt; would be entirely conformant to the 1.0 spec, though it wouldn't pass all the test cases.&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
