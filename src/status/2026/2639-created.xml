<item>
   <title>Issue #2639 created</title>
   <pubDate>2026-05-15T15:36:05Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2639</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2639</guid>
   <description>&lt;div&gt;&lt;p&gt;XQuery 4.0: automated review, open questions&lt;/p&gt;&lt;div class="markup"&gt;&lt;h2&gt;1. Switch comparison semantics (&lt;code&gt;expressions.xml&lt;/code&gt;, ~lines 22943–22947)&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;Note text:&lt;/strong&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;The comparisons are performed using the &lt;code&gt;fn:deep-equal&lt;/code&gt; function, after atomization.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;strong&gt;Formal description (step 4 of the same section):&lt;/strong&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;... compared using the contextually equal rules, with the default collation.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;strong&gt;Issue:&lt;/strong&gt; the note and the formal definition disagree on how case operands are compared. &lt;code&gt;fn:deep-equal&lt;/code&gt; is a structural-equality function; "contextually equal" is the value-equality comparison used by &lt;code&gt;eq&lt;/code&gt;. They give different results on e.g. atomized nodes vs. their string values.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Decision needed:&lt;/strong&gt; which comparison rule is intended? Most likely the formal definition is correct ("contextually equal") and the note is leftover from an earlier draft.&lt;/p&gt;
&lt;hr/&gt;
&lt;h2&gt;2. Partial function application rewrite (&lt;code&gt;expressions.xml&lt;/code&gt;, ~lines 11299–11300)&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;Current text:&lt;/strong&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;the result of &lt;code&gt;F(X, Y, ...)&lt;/code&gt; is &lt;code&gt;for $FI in F return $FI(X, Y, ...)&lt;/code&gt;.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;This sentence appears in the &lt;em&gt;partial-function-application&lt;/em&gt; section. The same rewrite, with the same wording, is also used in the dynamic-call section earlier (≈ line 10810). For ordinary dynamic calls it is correct.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Issue:&lt;/strong&gt; when one or more of &lt;code&gt;X&lt;/code&gt;, &lt;code&gt;Y&lt;/code&gt;, … is a placeholder (&lt;code&gt;?&lt;/code&gt;), the rewrite is wrong: the result is then a sequence of partially-applied functions, not the result of calling each function. The current text just defers to the dynamic-call rewrite, which does not handle placeholders.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Decision needed:&lt;/strong&gt; either drop this paragraph and add a forward reference to the actual partial-application semantics, or rewrite it explicitly with &lt;code&gt;?&lt;/code&gt; placeholders.&lt;/p&gt;
&lt;hr/&gt;
&lt;h2&gt;3. &lt;code&gt;for-each(., op('+'))&lt;/code&gt; example (&lt;code&gt;expressions.xml&lt;/code&gt;, ~line 24108)&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;Pipeline example:&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;(1 to 4)
=&amp;gt; for-each(., op('+'))
=&amp;gt; for-each-pair(4 to 7, ., op('&amp;gt;'))
=&amp;gt; some(.)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;described as "Doubles the values, then ...".&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Issue:&lt;/strong&gt; &lt;code&gt;fn:for-each&lt;/code&gt; expects a unary function but &lt;code&gt;op('+')&lt;/code&gt; produces a binary function. Either:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;the comment is wrong (this is not what doubles the values),&lt;/li&gt;
&lt;li&gt;the function name was meant to be different (e.g. &lt;code&gt;fn:fold-left&lt;/code&gt;), or&lt;/li&gt;
&lt;li&gt;the operator was meant to be &lt;code&gt;op('*2')&lt;/code&gt; / &lt;code&gt;fn($x) { $x + $x }&lt;/code&gt; / &lt;code&gt;fn($x) { $x * 2 }&lt;/code&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Decision needed:&lt;/strong&gt; what is this example supposed to demonstrate?&lt;/p&gt;
&lt;hr/&gt;
&lt;h2&gt;4. "no-namespace-rule" for pragma EQNames (&lt;code&gt;expressions.xml&lt;/code&gt;, ~line 24697)&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;Current text:&lt;/strong&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;The identifying EQName is expanded using the &lt;em&gt;no-namespace-rule&lt;/em&gt;.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;strong&gt;Issue:&lt;/strong&gt; in context, an EQName can already be a prefixed or &lt;code&gt;Q{…}name&lt;/code&gt; form, in which case the prefix / brace literal supplies the namespace. The no-namespace-rule only kicks in for an unprefixed NCName. The note immediately below this sentence already acknowledges this special case.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Decision needed:&lt;/strong&gt; reword to "If the EQName is an unprefixed NCName, it is interpreted using the no-namespace-rule" (or equivalent), so the broader case is handled by the normal EQName expansion rules.&lt;/p&gt;
&lt;hr/&gt;
&lt;h2&gt;5. &lt;code&gt;otherwise&lt;/code&gt; operator precedence (&lt;code&gt;expressions.xml&lt;/code&gt;, ~lines 22823–22826)&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;Current text:&lt;/strong&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;The &lt;code&gt;otherwise&lt;/code&gt; operator binds more tightly than comparison operators such as &lt;code&gt;=&lt;/code&gt;, but less tightly than string concatenation (&lt;code&gt;||&lt;/code&gt;) or arithmetic operators. The expression &lt;code&gt;$a = @x otherwise @y + 1&lt;/code&gt; parses as &lt;code&gt;$a = (@x otherwise (@y + 1))&lt;/code&gt;.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;strong&gt;Issue:&lt;/strong&gt; the precedence prose places &lt;code&gt;otherwise&lt;/code&gt; &lt;em&gt;between&lt;/em&gt; comparison and arithmetic operators, which is consistent with the example. However, the grouping &lt;code&gt;(@x otherwise (@y + 1))&lt;/code&gt; requires that &lt;code&gt;+&lt;/code&gt; bind tighter than &lt;code&gt;otherwise&lt;/code&gt; — which is what the prose says. So the example is consistent, but the wording "binds less tightly than … &lt;code&gt;||&lt;/code&gt; or arithmetic operators" lumps &lt;code&gt;||&lt;/code&gt; and &lt;code&gt;+&lt;/code&gt; together as a single precedence level, which is incorrect — &lt;code&gt;||&lt;/code&gt; is lower than &lt;code&gt;+&lt;/code&gt; in the grammar.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Decision needed:&lt;/strong&gt; clarify the precedence ordering; either split the bullet into separate clauses for string concatenation and arithmetic, or verify whether &lt;code&gt;otherwise&lt;/code&gt; is intended to sit between &lt;code&gt;||&lt;/code&gt; and arithmetic instead.&lt;/p&gt;
&lt;hr/&gt;
&lt;h2&gt;6. Ambiguous "otherwise" in finally clause (&lt;code&gt;expressions.xml&lt;/code&gt;, ~line 23329)&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;Current text:&lt;/strong&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;If it raises no error, it must yield the empty sequence; otherwise, a type error is raised XPTY0153.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;strong&gt;Issue:&lt;/strong&gt; "otherwise" is naturally read as "if it raises an error", but the intended condition is "if it raises no error &lt;em&gt;but&lt;/em&gt; the result is not the empty sequence". The current wording is misleading; the immediately preceding sentence already covers the error-raising case.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Decision needed:&lt;/strong&gt; reword to e.g. "If the finally clause completes without raising an error, its result must be the empty sequence; if it is not, a type error XPTY0153 is raised."&lt;/p&gt;
&lt;hr/&gt;
&lt;h2&gt;7. QName-literal example with unbound prefixes (&lt;code&gt;expressions.xml&lt;/code&gt;, ~lines 15461–15466)&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;Example:&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;QName("http://example.com/ns1", "this:color")
  eq QName("http://example.com/ns1", "that:color")
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;illustrating that QName equality ignores prefixes.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Issue:&lt;/strong&gt; &lt;code&gt;fn:QName&lt;/code&gt; requires the supplied prefix to be in the statically known namespaces of the call site (FONS0004). Without a &lt;code&gt;declare namespace this = …&lt;/code&gt; and &lt;code&gt;declare namespace that = …&lt;/code&gt; in the surrounding example context, this expression would raise an error, not return true.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Decision needed:&lt;/strong&gt; either add the necessary namespace declarations to the example, or rephrase the demonstration in a form that does not depend on prefix resolution.&lt;/p&gt;
&lt;hr/&gt;
&lt;h2&gt;8. Deprecated namespace constructor in example (&lt;code&gt;expressions.xml&lt;/code&gt;, ~line 18553)&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;Example:&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;namespace a { "http://a.example.com" }
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Issue:&lt;/strong&gt; the surrounding section (≈ 18353–18354, 18381–18384) introduces the &lt;code&gt;namespace #a { ... }&lt;/code&gt; form and presents the unhashed form as deprecated when the name is not a reserved keyword. Mixing both forms in adjacent examples is inconsistent.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Decision needed:&lt;/strong&gt; update the example to &lt;code&gt;namespace #a { "http://a.example.com" }&lt;/code&gt;, or explicitly note that this is the deprecated form.&lt;/p&gt;
&lt;hr/&gt;
&lt;h2&gt;9. &lt;code&gt;map:merge&lt;/code&gt; option example inconsistency (&lt;code&gt;query-prolog.xml&lt;/code&gt;, ~lines 2241, 2271, 2319)&lt;/h2&gt;
&lt;p&gt;The same example block describes &lt;code&gt;map:merge&lt;/code&gt; with three different option shapes:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;line 2241: &lt;code&gt;map:merge(({ "r": $r }, { "i": $i }))&lt;/code&gt; — no second argument&lt;/li&gt;
&lt;li&gt;line 2271: &lt;code&gt;map:merge(..., { "retain-order": true() })&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;line 2319 (formal): the default options map is &lt;code&gt;{ "duplicates": "use-first" }&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Issue:&lt;/strong&gt; the three are not obviously consistent. &lt;code&gt;"retain-order"&lt;/code&gt; is not mentioned in the canonical option set elsewhere.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Decision needed:&lt;/strong&gt; confirm the canonical option set in the current &lt;code&gt;map:merge&lt;/code&gt; signature (&lt;code&gt;xpath-functions-40&lt;/code&gt;) and update all three usages to match.&lt;/p&gt;
&lt;hr/&gt;
&lt;h2&gt;10. &lt;code&gt;my:rectangle&lt;/code&gt; example method calls (&lt;code&gt;query-prolog.xml&lt;/code&gt;, ~lines 2384–2387)&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;Example:&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;... =?=&amp;gt; expand(2)
... =?=&amp;gt; perimeter()
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;on an instance of the record type &lt;code&gt;my:rectangle&lt;/code&gt; declared at 2363–2375.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Issue:&lt;/strong&gt; the &lt;code&gt;my:rectangle&lt;/code&gt; record declaration only defines &lt;code&gt;area&lt;/code&gt; and &lt;code&gt;resize&lt;/code&gt;. &lt;code&gt;expand&lt;/code&gt; and &lt;code&gt;perimeter&lt;/code&gt; are not declared. Either the record needs more method declarations, or the example calls need to use the declared methods.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Decision needed:&lt;/strong&gt; which methods should the example demonstrate?&lt;/p&gt;
&lt;hr/&gt;
&lt;h2&gt;11. Atomic-set example clearly unfinished (&lt;code&gt;query-prolog.xml&lt;/code&gt;, ~lines 2466, 2468, 2498)&lt;/h2&gt;
&lt;p&gt;The block already has an &lt;code&gt;&amp;lt;ednote&amp;gt;&lt;/code&gt; (≈ 2514) noting the example is untested. Concrete defects spotted:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;2466: trailing &lt;code&gt;*&lt;/code&gt; in &lt;code&gt;record(...)&lt;/code&gt; suggests an extensible map type, but those were dropped from 4.0.&lt;/li&gt;
&lt;li&gt;2468: &lt;code&gt;declare %private variable DATA := "'_data'";&lt;/code&gt; is missing the &lt;code&gt;$&lt;/code&gt; before &lt;code&gt;DATA&lt;/code&gt;; later code references &lt;code&gt;$DATA&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;2498: stray &lt;code&gt;"&lt;/code&gt; and missing &lt;code&gt;,&lt;/code&gt; near &lt;code&gt;{ keys($set?$DATA) }"&lt;/code&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Decision needed:&lt;/strong&gt; rewrite the whole example block once the intended semantics is clear, or remove the example.&lt;/p&gt;
&lt;hr/&gt;
&lt;h2&gt;12. &lt;code&gt;errors.xml&lt;/code&gt; style consistency for DY/type-error pairs&lt;/h2&gt;
&lt;p&gt;A number of errors carry &lt;code&gt;class="DY" type="type"&lt;/code&gt; (XPDY0002, XPDY0050, XQDY0102 — and a handful of inline &lt;code&gt;&amp;lt;errorref&amp;gt;&lt;/code&gt; references that read "type error … XPDY0002"). The note at XPDY0002 explains the apparent inconsistency:&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;The error code is prefixed XPDY rather than XPTY for backwards compatibility reasons.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;The same explanation is &lt;em&gt;not&lt;/em&gt; attached to XPDY0050 and XQDY0102.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Decision needed:&lt;/strong&gt; decide whether to add an analogous note to the other affected entries, or to drop the explanation from XPDY0002 (treating the prefix convention as common knowledge after the introduction).&lt;/p&gt;
&lt;hr/&gt;
&lt;h2&gt;13. &lt;code&gt;doc.w3c-doctype-full = "Editor's Draft"&lt;/code&gt; vs. &lt;code&gt;doc.stage&lt;/code&gt; (&lt;code&gt;xpath.xml&lt;/code&gt;, &lt;code&gt;xquery.xml&lt;/code&gt;, ~lines 28–30)&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;Current:&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;!ENTITY doc.stage "WD"&amp;gt;                 &amp;lt;!-- xpath.xml --&amp;gt;
&amp;lt;!ENTITY doc.w3c-doctype "wd"&amp;gt;
&amp;lt;!ENTITY doc.w3c-doctype-full "Editor's Draft"&amp;gt;

&amp;lt;!ENTITY doc.stage "REC"&amp;gt;                &amp;lt;!-- xquery.xml --&amp;gt;
&amp;lt;!ENTITY doc.w3c-doctype "rec"&amp;gt;
&amp;lt;!ENTITY doc.w3c-doctype-full "Editor's Draft"&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Issue:&lt;/strong&gt; &lt;code&gt;doc.stage&lt;/code&gt; says WD (xpath) or REC (xquery), but &lt;code&gt;doc.w3c-doctype-full&lt;/code&gt; is "Editor's Draft" in both. "Editor's Draft" is not one of the W3C document stages listed in the comment header at the top of these files. The combinations look inconsistent in different ways for each file.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Decision needed:&lt;/strong&gt; which combination is intended for the next publication step? Specifically, should &lt;code&gt;doc.w3c-doctype-full&lt;/code&gt; track &lt;code&gt;doc.stage&lt;/code&gt; (e.g. "W3C Working Draft" or "W3C Recommendation"), or is "Editor's Draft" a deliberate override used while the spec is on the editor's-draft pipeline?&lt;/p&gt;
&lt;hr/&gt;
&lt;h2&gt;14. Doubled &lt;code&gt;!ENTITY&lt;/code&gt; definitions in &lt;code&gt;xpath.xml&lt;/code&gt; and &lt;code&gt;xquery.xml&lt;/code&gt;&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;Current (xpath.xml, similar pattern in xquery.xml):&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xml"&gt;&amp;lt;!ENTITY doc.publoc "https://qt4cg.org/specifications/&amp;amp;doc.shortname;/"&amp;gt;
&amp;lt;!ENTITY doc.publoc "&amp;amp;w3c.tr;/&amp;amp;date.year;/&amp;amp;doc.w3c-designation;-&amp;amp;doc.date;/"&amp;gt;

&amp;lt;!ENTITY doc.latestloc "&amp;amp;doc.publoc;"&amp;gt;
&amp;lt;!ENTITY doc.latestloc "&amp;amp;w3c.tr;/&amp;amp;doc.shortname;/"&amp;gt;

&amp;lt;!ENTITY doc.latestloc-tech "&amp;amp;doc.publoc;"&amp;gt;
&amp;lt;!ENTITY doc.latestloc-tech "&amp;amp;w3c.tr;/&amp;amp;doc.generic-shortname;/"&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Issue:&lt;/strong&gt; each entity is declared twice in succession. In XML DTDs the &lt;strong&gt;first&lt;/strong&gt; declaration wins; subsequent declarations for the same entity name are silently ignored. So either the duplication is dead code (the second definition never has any effect) or someone intended to toggle between alternatives by reordering — but the toggle is undocumented.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Decision needed:&lt;/strong&gt; clarify the intent — either pick one definition for each entity, or add a comment explaining the toggle convention. (A common W3C pattern is to comment out the inactive alternative with &lt;code&gt;&amp;lt;!--&lt;/code&gt; ... &lt;code&gt;--&amp;gt;&lt;/code&gt;.)&lt;/p&gt;
&lt;hr/&gt;
&lt;h2&gt;15. Namespace documents still on XQuery 3.1 (&lt;code&gt;ns-features.xml&lt;/code&gt;, &lt;code&gt;ns-local.xml&lt;/code&gt;, &lt;code&gt;ns-xquery.xml&lt;/code&gt;)&lt;/h2&gt;
&lt;p&gt;All three files have:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;title and prose referring to "XQuery 3.1 ... Namespace Document"&lt;/li&gt;
&lt;li&gt;date entities pinned to 2017-03-21 (XQuery 3.1 REC date)&lt;/li&gt;
&lt;li&gt;&lt;code&gt;xq.spec.ver "31"&lt;/code&gt; and references to &lt;code&gt;bibref ref="xquery-31-ref"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;For &lt;code&gt;ns-features.xml&lt;/code&gt; and &lt;code&gt;ns-xquery.xml&lt;/code&gt;: feature lists that still include
&lt;code&gt;qf:static-typing&lt;/code&gt; (the static-typing feature was dropped in 4.0,
conformance.xml 9–11) and &lt;code&gt;qf:module&lt;/code&gt; / &lt;code&gt;qfa:module&lt;/code&gt; (module processing is
no longer optional in 4.0, conformance.xml 12–15).&lt;/li&gt;
&lt;li&gt;For &lt;code&gt;ns-xquery.xml&lt;/code&gt; line 96: "Those additional &lt;strong&gt;applications&lt;/strong&gt; belong, by
default, to ..." should likely be "&lt;strong&gt;annotations&lt;/strong&gt;" (already flagged in the
review).&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Question:&lt;/strong&gt; the namespace URIs themselves were published in XQuery 3.1 and presumably remain stable, so the file's &lt;em&gt;identity&lt;/em&gt; (e.g. the 2011 date in the URI of &lt;code&gt;xquery-features&lt;/code&gt;) shouldn't change. But there are two open editorial questions:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Should the &lt;em&gt;documents&lt;/em&gt; be re-versioned/re-dated for the 4.0 publication cycle, or kept frozen as 3.1 artefacts?&lt;/li&gt;
&lt;li&gt;If re-versioned, the static-typing and module entries need to be removed from the optional-features lists.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;Decision needed:&lt;/strong&gt; WG decision on the lifecycle of these namespace documents.&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
