<item>
   <title>Issue #2845 created</title>
   <pubDate>2026-08-17T10:29:56Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2845</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2845</guid>
   <description>&lt;div&gt;&lt;p&gt;csv-to-xml: prose contradicts (broken) pseudo-code&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;The CSV rules seem to contradict the pseudo-code. In the pseudo-code, &lt;code&gt;&amp;lt;columns&amp;gt;&lt;/code&gt; is emitted whenever &lt;code&gt;exists($colNames)&lt;/code&gt;. The prose says…&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;If non-empty column names are available for some columns but not for others, then (a) the empty column element is included within the &lt;code&gt;columns&lt;/code&gt; element if and only if there is a subsequent column with a non-empty name, […]&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;The text was introduced with &lt;a href="https://github.com/qt4cg/qtspecs/commit/be078f51"&gt;be078f51&lt;/a&gt; as a resolution for https://github.com/qt4cg/qtspecs/issues/1675.&lt;/p&gt;
&lt;p&gt;I think the prose should win. The pseudo-code is broken anyway (&lt;code&gt;$colNames&lt;/code&gt; is declared, &lt;code&gt;$colnames&lt;/code&gt; is referenced; the rule from above is not implemented at all), perhaps:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;let $parsed := parse-csv($value, $options)
let $names  := $parsed?columns
let $last   := foot(index-where($names, fn { . != '' }))
return document {
  &amp;lt;csv xmlns="http://www.w3.org/2005/xpath-functions"&amp;gt; {
    if (exists($last)) {
      &amp;lt;columns&amp;gt;{ subsequence($names, 1, $last) ! &amp;lt;column&amp;gt;{ . }&amp;lt;/column&amp;gt; }&amp;lt;/columns&amp;gt;
    },
    &amp;lt;rows&amp;gt;{
      for $row in $parsed?rows
      return &amp;lt;row&amp;gt;{
        for member $field at $col in $row
        return &amp;lt;field&amp;gt;{
          if ($names[$col] != '') { attribute column { $names[$col] } },
          $field
        }&amp;lt;/field&amp;gt;
      }&amp;lt;/row&amp;gt;
    }&amp;lt;/rows&amp;gt;
  }&amp;lt;/csv&amp;gt;
}

&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
