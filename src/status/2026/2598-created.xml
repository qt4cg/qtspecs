<item>
   <title>Issue #2598 created</title>
   <pubDate>2026-04-28T10:48:53Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2598</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2598</guid>
   <description>&lt;div&gt;&lt;p&gt;Add regex() function&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;Add a regex($pattern, $flags) function to construct a "compiled" regular expression. This gives a clear signal that the same regular expression might be used repeatedly; it avoids relying on caching, which can be unnecessarily expensive and is not predictable across implementations.&lt;/p&gt;
&lt;p&gt;Two possible ways this might work:&lt;/p&gt;
&lt;p&gt;(a) allow the result of the regex() function to be used as an argument to functions such as matches(), tokenize(), etc, in place of the &lt;code&gt;$pattern&lt;/code&gt; and &lt;code&gt;$flags&lt;/code&gt; arguments&lt;/p&gt;
&lt;p&gt;(b) make the regex() function return a record with methods matches(), tokenize(), replace() etc whose functionality corresponds to the existing functions.&lt;/p&gt;
&lt;p&gt;The second approach seems easier to fit into the type system. With the first approach, it's unclear what kind of value the regex() function should return.&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
