<item>
   <title>Issue #2433 created</title>
   <pubDate>2026-02-03T06:38:29Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2433</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2433</guid>
   <description>&lt;div&gt;&lt;p&gt;`fn:jtree`: Identity&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;The rules for &lt;code&gt;fn:jtree&lt;/code&gt; say:&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;If two maps or arrays M1 and M2 have the same function identity, as determined by the &lt;code&gt;function-identity&lt;/code&gt; function, then &lt;code&gt;jtree(M1) is jtree(M2)&lt;/code&gt; MUST return true: that is, the same JNode must be delivered for both.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;blockquote&gt;
&lt;p&gt;Note: It is to some extent implementation-defined whether two maps or arrays have the same function identity. Processors SHOULD ensure as a minimum that when a variable &lt;code&gt;$m&lt;/code&gt; is bound to a map or array, calling &lt;code&gt;jtree($m)&lt;/code&gt; more than once (with the same variable reference) will deliver the same JNode each time.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;Shouldn’t SHOULD be MUST? The argument will always have the same function identity if &lt;code&gt;jtree($m)&lt;/code&gt; is called more than once. Maybe the note can also be dropped, as &lt;code&gt;fn:jtree&lt;/code&gt; is not about the identity of maps and arrays, but the identity of JNodes.&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
