<item>
   <title>Issue #2853 created</title>
   <pubDate>2026-08-20T10:48:01Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2853</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2853</guid>
   <description>&lt;div&gt;&lt;p&gt;Default values of function parameters&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;XSLT and XQuery have slightly different rules for how context dependencies are handled in expressions beng used to define the default values of function parameters. For example, if the default is given as "static-base-uri()", then I don't think XQuery makes it clear whether this is the static base URI of the function definition, or the static base URI of the caller.&lt;/p&gt;
&lt;p&gt;Variable references within the default value expression are particularly problematic and I think we should make it explicit that they are not allowed. We could allow references to global variables but even that is complicated because of visibility issues if the variable is not visible to the caller. XSLT on the current rules gets into a complete mess if the default value is given as "$var" when the static context of the function definition and the dynamic context of the caller include different variables both called "$var".&lt;/p&gt;
&lt;p&gt;It's fairly obvious, I think, that namespaces should be resolved using the static context of the function definition: but I'm not sure that XQuery says so explicitly. Resolving function references such as fn:true#0 should also use the static context of the function definition. But what about a call to function-lookup()?&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
