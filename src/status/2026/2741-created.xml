<item>
   <title>Issue #2741 created</title>
   <pubDate>2026-06-30T13:07:57Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2741</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2741</guid>
   <description>&lt;div&gt;&lt;p&gt;Record coercion, drop/keep empty entries?&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;#2566 says that optional fields in records are to be &lt;em&gt;dropped&lt;/em&gt; (there is “no longer a distinction between absent values and empty values”). But the &lt;code&gt;TypedRecordType&lt;/code&gt; coercion rule still adds an empty entry for every omitted field, so a coerced record keeps all its fields. The two seem to be mutually exclusive.&lt;/p&gt;
&lt;p&gt;An example…&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;let $x as record(a as xs:integer, b as xs:string?) := { "a": 1 }
return map:size($x)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Coercion rule: &lt;code&gt;2&lt;/code&gt; (with &lt;code&gt;b&lt;/code&gt; added as &lt;code&gt;()&lt;/code&gt;); with #2566: &lt;code&gt;1&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;…and the conflicting text snippets:&lt;/p&gt;
&lt;p&gt;#2566 change note, &lt;em&gt;Record Types&lt;/em&gt;:&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;Optional fields in records are dropped (in records, there is no longer a distinction between absent values and empty values).&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;Record-construction prose, same section:&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;The rules for the coercion operation ensure that the resulting record has one entry for every field in the corresponding record definition …&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;Coercion rules, &lt;code&gt;TypedRecordType&lt;/code&gt;, the "Otherwise" clause:&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;Otherwise (if J contains no such entry), an entry whose key is the name of F and whose associated value is the empty sequence; a type error occurs if the empty sequence does not match the declared type of F.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
