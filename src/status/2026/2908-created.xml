<item>
   <title>Issue #2908 created</title>
   <pubDate>2026-09-13T20:04:05Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2908</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2908</guid>
   <description>&lt;div&gt;&lt;p&gt;Named, Unnamed, and Recursive record types&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;The subtyping rules currently make a distinction between recursive record types (which are necessarily named types) and non-recursive types (which may be named or unnamed). This permeates through to the rules for matching types against instances, which are defined in terms of the subtyping rules.&lt;/p&gt;
&lt;p&gt;It's not totally clear which rules apply: for example does 3.3.2.9 [Subtyping Non-recursive Record Types] only apply when both types are non-recursive? What are the rules when one type is recursive and the other is not?&lt;/p&gt;
&lt;p&gt;In addition it seems awkward, and likely to lead to confusion, if the rules are different depending on whether the definition is recursive: one can imagine this changing over time as the type is refined, and being recursive is a property that may not immediately be apparent to the reader when the chain of references is long.&lt;/p&gt;
&lt;p&gt;I think we may get more consistent and predictable results if we draw the distinction between named and unnamed record types, rather than between recursive and non-recursive types (so non-recursive named types behave like recursive named types, minus the recursion). We can then use "named typing" for named types, and "structural typing" for unnamed types.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;For named record types the identity of a type is essentially its name (modulo rules that allow the same name to be used for different types in different modules). Rules for "instance of" and "subtype of" are then based on type names: a record is an instance of a named record type if it is annotated with that type name, and one named record type is a subtype of another if it is explicitly defined as such.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;For unnamed record types, the identity of a type is essentially its definition, and "instance of" and "subtype of" relationships are determined by comparing structure.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A named record type can then be defined to be a subtype of the corresponding unnamed type (it imposes an additional constraint on instances, namely that the type name must match).&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;But I'm inclined to be more radical: eliminate unnamed record types. It's probably good software engineering practice (DRY principles) to define types once and give them a name that reflects their semantic purpose, and it certainly simplifies the rules.&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
