<item>
   <title>Issue #2765 created</title>
   <pubDate>2026-07-05T11:38:27Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2765</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2765</guid>
   <description>&lt;div&gt;&lt;p&gt;Destructuring let: records&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;The current spec says:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;let ${ $a, $b as xs:integer, $local:c } := { "a": 2, "b": 4, "c": 6, "d": 8 }
return $a + $b + $local:c
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;…is expanded to:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;let $temp := { "a": 2, "b": 4, "c": 6 }
let $a := map:get($temp, "a", ())
let $b as xs:integer := map:get($temp, "b", ())
let $local:c := map:get($temp, "c", ())
return $a + $b + $local:c
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;We should use the lookup operator or a dynamic function call, so that &lt;code&gt;let ${unknown}&lt;/code&gt; results in an error.&lt;/p&gt;
&lt;p&gt;A side note, the rules for arrays say:&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;… replaced by a LetValueBinding of the form &lt;code&gt;let Ai as Ti := array:get(V, i)&lt;/code&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;The subsequent example uses &lt;code&gt;array:get($temp, 1, ())&lt;/code&gt;.&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
