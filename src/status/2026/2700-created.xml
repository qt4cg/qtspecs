<item>
   <title>Issue #2700 created</title>
   <pubDate>2026-06-22T16:44:07Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2700</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2700</guid>
   <description>&lt;div&gt;&lt;p&gt;Map constructor: create map entries for items other than maps/JNodes&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;In many scenarios, comparisons can be sped up a lot by replacing sequences with maps. For example, the following code might take O(n²) if a processor should fail to use a hash join at runtime:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;let $known := $old
return $new[. = $known]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;With the current 4.0 map constructor syntax &lt;code&gt;{ EXPR }&lt;/code&gt;, the example from above can be rewritten as follows:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;let $known := { $old ! map:entry(., true()) }
return $new[map:contains($known, .)]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;We could improve the status quo by automatically creating map entries with &lt;code&gt;data(ITEM)&lt;/code&gt; as key and &lt;code&gt;true()&lt;/code&gt; as value for each item which is no JNode or map. This would allow us to write:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;let $known := { $old }
return $new[$known(.)]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;I believe the compact syntax could be helpful in many scenarios where maps are used as sets. Of course one could write &lt;code&gt;map:build($old)&lt;/code&gt; if all EBV of the items are known to be true, but replacing round by curly brackets seems so much nicer.&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
