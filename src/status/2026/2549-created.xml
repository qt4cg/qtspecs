<item>
   <title>Issue #2549 created</title>
   <pubDate>2026-03-22T22:37:34Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2549</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2549</guid>
   <description>&lt;div&gt;&lt;p&gt;Pipelining file operations&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;I pointed out in #2408 that this example&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;'test.bin'
=&amp;gt; file:write-binary(xs:hexBinary('00'))
=&amp;gt; file:read-text(fallback := true())
=&amp;gt; string-to-codepoints()
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;doesn't work, because file:write-binary() returns an empty sequence.&lt;/p&gt;
&lt;p&gt;This raises the question of whether there is any way of writing this that is guaranteed to perform the &lt;code&gt;write-binary()&lt;/code&gt; before the &lt;code&gt;read-text()&lt;/code&gt;, and I don't think there is. This affects all the file functions that are called for their side effects. Many implementations are likely to evaluate&lt;/p&gt;
&lt;p&gt;&lt;code&gt;(file:write-binary(), file:read-text())&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;sequentially, but if you rely on this you're at the mercy of the optimizer.&lt;/p&gt;
&lt;p&gt;One solution would be to make these functions return the filename, which would make the above example work: but this would not be backwards compatible. A solution to that might be to define a variant of the function that does this (e.g. a function with a different name, or with an extra parameter).&lt;/p&gt;
&lt;p&gt;Another solution might be to define a construct&lt;/p&gt;
&lt;p&gt;&lt;code&gt;do {X, Y,  Z}&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;that is defined to evaluate X, Y, and Z in that order and return the result of Z.&lt;/p&gt;
&lt;p&gt;Of course a pure functional solution would get into the area of monads.&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
