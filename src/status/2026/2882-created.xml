<item>
   <title>Issue #2882 created</title>
   <pubDate>2026-08-31T23:33:06Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2882</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2882</guid>
   <description>&lt;div&gt;&lt;p&gt;load-xquery-module: functions with context-sensitive defaults&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;The spec for load-xquery-module says (in a Note!)&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;If a function declaration F in the loaded module declares (say) four parameters of which one is optional, its arity range will be from 3 to 4, so the result will include two function items corresponding to F#3 and F#4. In the lower-arity function item, F#3, the fourth parameter will take its default value. If the expression that initializes the default value is context sensitive, the static and dynamic context for its evaluation are the static and dynamic contexts of the [fn:load-xquery-module] function call itself.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;This is inconsistent with the way the evaluation context for function parameter defaults now works. The context should be the same as for initializers of global variables, for example a default of "." picks up the context item/value defined in the options to load-xquery-module.&lt;/p&gt;
&lt;p&gt;If the default refers to &lt;code&gt;fn:current()&lt;/code&gt; then I guess this should be bound to the context item for the evaluation of load-xquery-module.&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
