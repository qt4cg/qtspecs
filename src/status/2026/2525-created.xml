<item>
   <title>Issue #2525 created</title>
   <pubDate>2026-03-16T13:30:27Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2525</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2525</guid>
   <description>&lt;div&gt;&lt;p&gt;JNodes: map constructor&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;The first feedback on JNodes shows that people find the analogy between XML and maps/arrays really attractive. At the same time, it’s perhaps the most challenging part, as the subtle differences of the two data types need to be understood.&lt;/p&gt;
&lt;p&gt;For example, our constructors behave differently: Whereas the node constructor includes the full node in the result…&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;let $xnode := document { &amp;lt;n&amp;gt;123&amp;lt;/n&amp;gt; }
return document { $xnode/n },

(: result :)
document { &amp;lt;n&amp;gt;123&amp;lt;/n&amp;gt; }
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;…the map constructor includes only the ·jvalue· and ignores the ·jkey·, such that the following query fails:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;let $jnode := { 'n': 123 }
return { $jnode/n }
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Maybe we can tweak map constructors by generating single-entry maps from non-root JNodes?&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;let $jnode := { 'n': 123 }
return { $jnode/n }
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This would enable us to do many other things, such as filtering:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;let $jnode := {
  'lu': 'lunes',
  'ma': 'martes',
  'mi': 'miércoles',
  'ju': 'jueves'
}
return { $jnode/(lu, mi) }

(: result :)
{ 'lu': 'lunes', 'mi': 'miércoles' }
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;In addition, if required, we could introduce an explicit function to convert a JNode to a single-entry map (basically, a shortcut for &lt;code&gt;map:entry(jkey(), jvalue())&lt;/code&gt;.&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
