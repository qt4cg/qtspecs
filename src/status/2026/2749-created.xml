<item>
   <title>Issue #2749 created</title>
   <pubDate>2026-07-02T10:13:27Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2749</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2749</guid>
   <description>&lt;div&gt;&lt;p&gt;Map constructors: poor diagnostics&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;The change to map constructors to allow an expression (returning a map or JNode) in place of a (keyword: value) pair can make it very difficult to produce good diagnostics for simple errors. For example, I accidentally wrote:&lt;/p&gt;
&lt;p&gt;&lt;code&gt;declare %public variable $lib:dateTimeNow as lib:dateTimeSupplier := fn(){{"date": current-date#0, "time", current-time#0}};&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;leading to the error message&lt;/p&gt;
&lt;p&gt;&lt;code&gt;XPTY0004  The required item type of the second entry in the map constructor is (map(*)|jnode())&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;The actual error is using "," in place of ":".&lt;/p&gt;
&lt;p&gt;I'm wondering if it might be better to require the user to signal their intent when using an expression that returns map entries, for example in place of&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{ "monday": "work",
  if ($weekends) {
     {"saturday": "football"}
  }
}

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;require:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{ "monday": "work",
  + :  if ($weekends) {
     {"saturday": "football"}
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
