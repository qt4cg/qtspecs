<item>
   <title>Issue #2615 created</title>
   <pubDate>2026-05-05T16:08:05Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2615</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2615</guid>
   <description>&lt;div&gt;&lt;p&gt;XSLT: should xsl:map-entry return a JNode?&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;Following on from issue #2613 (and PR #2614), should we make &lt;code&gt;xsl:map-entry&lt;/code&gt; return a JNode?&lt;/p&gt;
&lt;p&gt;This instruction is present in XSLT 3.0, and it is defined to return a single-entry map. The xsl:map instruction expects a sequence of maps as input, and it merges them. It would be easy enough to change xsl:map-entry to return a JNode, and xsl:map to accept that JNode, but there would be a backwards incompatibility for any application that uses the result of xsl:map-entry in any way other than to construct input to xsl:map.&lt;/p&gt;
&lt;p&gt;There are in fact two ways we could change xsl:map-entry to return a JNode. We could return a JNode whose &lt;code&gt;jvalue&lt;/code&gt; property is a single-entry map, or we could change it so that the &lt;code&gt;jkey&lt;/code&gt; property is the relevant key and the &lt;code&gt;jvalue&lt;/code&gt; property is the corresponding value.&lt;/p&gt;
&lt;p&gt;We need to examine what the benefits of making this change would be, especially when it comes to doing "shallow-copy" template-based transformations of a tree of maps and arrays.&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
