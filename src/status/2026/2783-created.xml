<item>
   <title>Issue #2783 created</title>
   <pubDate>2026-07-12T19:32:09Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2783</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2783</guid>
   <description>&lt;div&gt;&lt;p&gt;Padding strings&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;A simple one:&lt;/p&gt;
&lt;p&gt;XPath can format numbers and dates, but it cannot pad a string to a fixed width, although it would often be helpful when creating textual output (reports, log lines, etc.). That’s something most other languages provide.&lt;/p&gt;
&lt;p&gt;Users must write it themselves, and the usual workarounds are easy to get wrong. &lt;code&gt;substring($s || '     ', 1, 5)&lt;/code&gt; only works if the literal is long enough, and it truncates. &lt;code&gt;$s || string-join(replicate(' ', 5 - string-length($s)))&lt;/code&gt; needs two functions and does not feel very intuitive.&lt;/p&gt;
&lt;p&gt;The signature, as I would draft it:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;fn:pad-string(
  $value    as item()?,
  $length   as xs:integer,
  $options  as map(*)? := {}
) as xs:string
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The options could be &lt;code&gt;fill&lt;/code&gt; (default &lt;code&gt;" "&lt;/code&gt;) and side (&lt;code&gt;end&lt;/code&gt; by default, &lt;code&gt;start&lt;/code&gt;, &lt;code&gt;both&lt;/code&gt;). Examples:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;pad-string('abc', 10)                                  (: "abc       " :)
pad-string(42, 6, { 'fill': '0', 'side': 'start' })    (: "000042" :)
pad-string('Chapter 1', 20, { 'fill': '.' })           (: "Chapter 1..........." :)
&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
