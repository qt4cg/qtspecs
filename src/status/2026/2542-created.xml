<item>
   <title>Issue #2542 created</title>
   <pubDate>2026-03-20T10:16:06Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2542</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2542</guid>
   <description>&lt;div&gt;&lt;p&gt;Function signature of fn:subsequence&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;I propose that we change the argument type of the second and third arguments of fn:subsequence from xs:double to xs:numeric.&lt;/p&gt;
&lt;p&gt;These arguments are xs:double for legacy reasons; if we defined it today they would be xs:integer. In practice the supplied arguments will almost always be integers, except for some peculiar 1.0 constructs where it's handy to supply infinity or NaN. If the function is implemented as declared, then a supplied integer gets converted to a double and the implementation typically has to convert it back. But if the implementation declares the type as xs:numeric (as Saxon does), then in edge cases involving function subtyping it's non-conformant.&lt;/p&gt;
&lt;p&gt;There is a minor incompatibility. In 1.0 compatibility mode, the call &lt;code&gt;subsequence($s, 1, "INF")&lt;/code&gt; is currently accepted because the expected type is &lt;code&gt;xs:double&lt;/code&gt; so "INF" gets treated as &lt;code&gt;number("INF")&lt;/code&gt;; supplying a string isn't allowed if the type is declared as &lt;code&gt;xs:numeric&lt;/code&gt;. However, Saxon has been using &lt;code&gt;xs:numeric&lt;/code&gt; as the required type for years (I checked back to 9.6), and this has never been raised as a problem.&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
