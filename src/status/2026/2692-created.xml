<item>
   <title>Issue #2692 created</title>
   <pubDate>2026-06-16T17:37:11Z</pubDate>
   <link>https://github.com/qt4cg/qtspecs/issues/2692</link>
   <guid>https://qt4cg.org/@qt4cg/2026/#created-2692</guid>
   <description>&lt;div&gt;&lt;p&gt;Destructuring inconsistencies and questions&lt;/p&gt;&lt;div class="markup"&gt;&lt;p&gt;These questions came up during the XQuery 4 tutorial held on XML Prague 2026&lt;/p&gt;
&lt;h2&gt;Question 1: Can destructured variables have a sequence type?&lt;/h2&gt;
&lt;p&gt;Is it possible to declare the (sequence)type when destructuring sequences into items.&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;let $($a as xs:positiveInteger+, $b as numeric()*) := (1, 2.9, -3, xs:double("Inf"))
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;If this is possible do we have to raise errors must be raised for&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;let $($a as xs:string, $b as numeric()) := (1, 2.9, -3, xs:double("Inf"))
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Is the below construct possible?&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;let $($a, $b) as numeric()* := (1, 2.9, -3, xs:double("Inf"))
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Question 2: Destructuring arrays and excess members&lt;/h2&gt;
&lt;p&gt;Given a code like &lt;code&gt;let $[$a, $b] := [1, 2, 3]&lt;/code&gt;
The current implementation in BaseX will discard any excess members in the array. There is no way to destructure the &lt;em&gt;rest&lt;/em&gt; of the array members. This is different from the sequence destructuring.&lt;/p&gt;
&lt;p&gt;Should this&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;raise an error - as it does in python - as excess array members would be discarded?&lt;/li&gt;
&lt;li&gt;should &lt;code&gt;$b&lt;/code&gt; instead select (2, 3) so that the last destructuring variable selects the tail end of all members not yet selected?
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;let $[$all] := [1, 2, 3]&lt;/code&gt; would be equivalent to &lt;code&gt;let $all := [1, 2, 3]?*&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Question 3: Destructuring in function parameters&lt;/h2&gt;
&lt;p&gt;It is quite common in JavaScript to destructure object arguments into only those properties that are actually needed.
Here is a rather theoretical example how that could look like in XQuery&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;declare function nextX(${$x}) { $x + 1 };

nextX({"x": 1, "y": 1})
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Question 4: Renaming map entries in map destructuring&lt;/h2&gt;
&lt;p&gt;This is a
Adding two complex numbers could be achieved with a renaming destructuring operation.&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-xquery"&gt;declare record complex:number(r as xs:double, i as xs:double);
declare function complex:add (${$x:r, $y:i}, ${$u:r, $v:i}) {
    complex:number($x + $y, $u + $v)
};
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Renaming could also allow desctructuring of maps with keys other than NCNames.&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</description>
</item>
